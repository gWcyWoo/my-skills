# Discovery — no specific symbol known

1. Probe search_code with domain keywords, scoped to likely directory.
2. Once you have a file/function → switch to Pinpoint or Trace (Read the corresponding file).
3. Probe unavailable → Grep with keyword patterns as fallback.
