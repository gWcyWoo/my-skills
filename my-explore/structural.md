# Structural — find all code matching a pattern

## Steps

1. **Search by code structure** — ast-grep find_code or find_code_by_rule.
2. **Narrow results** — add `inside`/`has` constraints if too many matches.
3. **Analyze imports** — ast-grep analyze-imports for dependency analysis.
