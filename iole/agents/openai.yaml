interface:
  display_name: "IOLE"
  short_description: "Title-linked single or multi-page engineering flow"
  default_prompt: "Use $iole with this shared task sheet and role=client, discovering child pages from unique title references."
