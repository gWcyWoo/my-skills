# dpt edge / snippets/modsec.conf —— 每 vhost 启用 WAF(默认 DetectionOnly,见 modsec/modsecurity.conf)。
# 占位思路:业务 vhost include 它即可;调稳前 SecRuleEngine 保持 DetectionOnly。
modsecurity on;
modsecurity_rules_file /etc/nginx/modsec/main.conf;
