# dpt edge / snippets/ratelimit.conf —— 每 IP 限速 + 并发限制(server/location include)。
# 区在 nginx.conf 的 http{} 定义。登录/API 想更严就单独再 include 一份更小 rate 的。
limit_req  zone=req_per_ip  burst=20 nodelay;
limit_conn conn_per_ip 20;
