# dpt edge / modsec/main.conf —— ModSecurity 规则入口(被 snippets/modsec.conf 引用)。
# modsecurity.conf 与 unicode.mapping 由 install_waf 从 ModSecurity 上游取(含 SecRuleEngine DetectionOnly)。
Include /etc/nginx/modsec/modsecurity.conf
Include /etc/nginx/modsec/crs/crs-setup.conf
Include /etc/nginx/modsec/crs/rules/*.conf
