# c3-v2 evidence dump (initial, ts=1777820571)

## Codex-side (`~/.agents/skills/c3/`)

### `scripts/wait_for_turn.py`
- :86-98 — main loop: `while True: ... time.sleep(args.poll_seconds)`. Default `--poll-seconds 30.0`. **Polling, not event-driven.**
- :19-42 — `parse_turns(text)` — header regex `^- {ts} To (claude|codex):$`, case-insensitive.
- :45-49 — `is_done(turn)` — strips empty lines, checks tail line equals `- done`.
- :52-62 — `latest_complete_inbound(path, recipient)` — only returns LATEST complete turn, gated by `latest_turn["to"] == recipient`. Means "we already replied" case is naturally filtered (LAST is To codex, not To claude → returns None).
- :70-98 — CLI: `transcript`, `recipient`, `--poll-seconds 30.0`, `--timeout-seconds 0.0` (0 = wait forever). On timeout: stderr + exit 2.

### `scripts/append_turn.py`
- :11-17 — `format_turn(recipient, message)` — auto-adds timestamp, indents bullets with 2 spaces, appends `  - done`.
- :20-42 — CLI: `conversation`, `recipient` (choices=[codex, claude]), positional `message` OR `--message-file` flag. **Confirmed: `--message-file` reads content from file path, eliminating shell-arg escaping issue.**
- :37 — `needs_blank` — inserts blank separator if file non-empty.

### `SKILL.md` (~119 lines)
- :17-26 — start flow: write target.md, create conversation.md, run u-0 (sub-skill), append first turn.
- :47-50 — only mentions `append_turn.py` and `wait_for_turn.py --poll-seconds 30` as canonical I/O paths.
- :85-93 — implementation phase: Codex must wait via `wait_for_turn.py` while Claude implements; "do not stop waiting".
- **No mention** of `/loop`, `ScheduleWakeup`, exit-1 codepath, `wf` protocol, or `notes-{ts}.md` evidence files.

## Claude-side (`~/.claude/skills/c3/`)

### `c3-io.sh`
- :41-72 — mode `r`: awk parses LAST top-level block. Exit 0 + prints iff block is `To Claude:` AND ends with `- done`. Else exit 1. **Non-blocking.**
- :75-88 — mode `w`: takes `<content>` as positional shell arg. Shell-escaping nightmare for blocks with `$` / quotes / backticks → forced the heredoc workaround that triggered permission prompts.
- :90-108 — mode `wf` (added in this session): takes `<blockfile>` path, reads content from file, appends. Eliminates shell-escaping problem.

### `SKILL.md` (~115 lines)
- :7 — role line specifies tick model: `c3-io.sh r → exit 1 → ScheduleWakeup → return; exit 0 → process → wf → ScheduleWakeup`.
- :32-40 — I/O helper section + `wf` 3-step protocol description (Write `/tmp/c3_block_{ts}.txt` → bash wf → rm).
- :58-79 — instructions wrapped in `/loop /c3 <session>`. Steps 0–5. Multiple `ScheduleWakeup(30)` calls.
- :101-112 — P0 reminders: 9 of them, several about `wf` protocol / heredoc forbiddance / ScheduleWakeup discipline.

## Claude Code harness facts (from Bash tool docs in this session)
- Bash tool default timeout: `120000ms` (2 min). Max: `600000ms` (10 min).
- Bash tool supports `run_in_background: true` — returns shell ID immediately, harness sends `<task-notification>` when subprocess exits.
- ScheduleWakeup runtime: clamps `delaySeconds` to `[60, 3600]`. `ScheduleWakeup(30)` actually fires at 60s, **silently**. Confirmed empirically earlier this session: requested 30s → response "clamped to 60s from your requested value".

## Conclusion (initial position)
- Codex's `wait_for_turn.py` is already much closer to v2 than my `c3-io.sh r`. **Build on top of it.**
- Codex's `append_turn.py --message-file` already solves the write-side escaping problem. **Adopt instead of `wf` 3-step.**
- Main remaining gap: replace `time.sleep(30)` polling with fsevent-driven blocking (Python `watchdog`).
