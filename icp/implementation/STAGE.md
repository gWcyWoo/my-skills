# Stage 3 — implementation(规范,未实现)

> 只有契约。实现前置条件:模拟器采集回路先通过独立冒烟测试(启动→安装→截屏→dump视图树→退出,全程无人工),这是全链路最脆弱环节,必须先于任何页面生成单独验证。

## 输入
Stage 2 全部冻结产物 + 目标仓库(分支基线哈希记录在案)。

## 输出与不变量
- TDD:每个交互原子先落 RED 测试再实现;GREEN 后不得删除或弱化断言(测试文件哈希入账)。
- 运行时探针:页面在模拟器实际渲染,视图树与 `component-spec` 对照(组件出现性、层级、文案),截图入档。
- 失败归因账本:每次失败必须归类 semantic(回Stage 1/2)| codegen(本阶段修)| environment(记录后重试),归因写入 `attribution-ledger.json`;禁止无归因重试。
- 禁令:不得新增交互原子;缺失 → `blocking_stage2_defect` 停止。

## 交付
mr=0 仅本地产物;mr=1 提交分支;mr=2 开 MR。默认从 mr=0 开始,升档由 IOLE 控制。
