#!/bin/sh
set -eu

for candidate in "$HOME"/.gradle/wrapper/dists/gradle-9.4.1-bin/*/gradle-9.4.1/bin/gradle; do
    if [ -x "$candidate" ]; then
        exec "$candidate" --offline "$@"
    fi
done

exec gradle --offline "$@"
